# 听写跟读功能实现说明

## 目标

> **2026-07-16 产品范围调整**：去掉中文语音与听写；保留英文点读、英文跟读、人教高中词库。

实现「选教材/单元 → 单词列表英文点读 → 英文跟读」，支持语速、英文重复遍数、词间隔、列表打乱。纯浏览器，无后端。

## 技术选型

| 项 | 选择 | 原因 |
|----|------|------|
| 框架 | Vite + React + TypeScript | 轻量、本地静态部署 |
| TTS | Web Speech API `speechSynthesis` | 零依赖、中英分 voice、可调 rate |
| 持久化 | localStorage | 设置与导入教材刷新不丢 |

未采用 TypeWords / Qwerty Learner 整仓 fork：主流程是打字背词，与「中文听写流水线」不贴合；GPL 体量大。仅参考其词库章节思路。

## 核心模块

### 1. `src/lib/tts.ts`

- `speak(text, { lang, rate })`：朗读；手机优先**在线 TTS**（英文有道、中文百度等），失败再回退系统 `speechSynthesis`
- `unlockSpeech()`：在 touch/click 手势内解锁 iOS/Android 音频
- `cancelSpeak()`：同时停掉 Audio 与 speechSynthesis
- 系统语音：`cancel` 后延迟 50ms 再 `speak`，并 `resume` 防 Chrome 卡住

### 已知问题：手机点击无声（已修）

| 原因 | 处理 |
|------|------|
| 系统未装中文/英文语音包 | 手机改走在线 TTS |
| iOS 取消后立刻 speak 静音 | cancel → 50ms → speak |
| 未在用户手势中解锁音频 | `unlockSpeech` + pointerdown |
| 仅依赖 Web Speech 在微信等 WebView 失效 | HTMLAudio 播放在线发音 URL |

文档：

- [`docs/bug-手机点击无声音.md`](./bug-手机点击无声音.md)  
- [`docs/bug-英文秒出中文不行.md`](./bug-英文秒出中文不行.md) — **为何英文快、中文慢/失败**（有道词典音 vs 百度中文 TTS）  
- [`docs/方案-中文音频预生成.md`](./方案-中文音频预生成.md) — 中文 mp3 预生成（**已搁置**）  
- [`docs/词库资源-检索记录.md`](./词库资源-检索记录.md) — 人教版高中英语词库来源检索  
- [`docs/bug-词库版本与预备单元.md`](./bug-词库版本与预备单元.md) — 旧课标误用、缺少 Welcome Unit 的问题与修复  
- [`docs/bug-单元默认全选.md`](./bug-单元默认全选.md) — 换册默认全选单元 → 改为默认不选  
- [`docs/code-review-2026-07-16.md`](./code-review-2026-07-16.md) — 代码审查问题与解决方案（已全部修复）

### 2. `src/lib/queue.ts` — `WordPlayer`

状态机：

```
start(words, mode) → buildQueue(shuffle?)
  for each word:
    dictation: speak(zh) × repeatZh
    shadow:    speak(en) × repeatEn
    wait(gapMs)  // 最后一词后不等
  speak("本单元结束")
  status = done
```

控制：`pause`（cancel + 清 gap）、`resume`、`skip`（索引+1）、`stop`。

### 3. `src/lib/import.ts`

- JSON：校验 id/title/units/words
- CSV：表头 unit,en,zh（中英别名），按 unit 聚合

### 4. `src/App.tsx`

- 教材选择、单元多选、三模式、设置滑块、单词表双按钮
- 听写时可选隐藏英文（纸笔默写）
- **打乱单词列表**：`shuffleArray` 重排 `displayWords`；「恢复顺序」回到教材单元合并顺序；听写/跟读按当前列表播放

## 手机网页适配（2026-07-16）

- **手机优先**：默认单列、最大宽约 720px，触控高度 ≥44px  
- 输入框 `font-size: 16px`，避免 iOS 聚焦放大页面  
- 单词由表格改为**卡片**，「英」「中」大按钮  
- 播放中**底部固定控制条**（安全区 `safe-area-inset`），便于单手暂停/下一词  
- 「语速/遍数」折叠面板，减少首屏占用  
- 练习区置顶，先点听写/跟读再滚选教材  

手机访问：同一局域网下用电脑 `npm run dev -- --host`，手机打开显示的 Network 地址。

## 使用方式

```bash
npm install
npm run dev
# 手机访问需暴露局域网：
npm run dev -- --host
```

浏览器打开终端提示的本地地址（默认 `http://localhost:5173`）。

1. 选择「示例教材」或导入 JSON/CSV  
2. 勾选单元  
3. **点读**：列表点「英」「中」  
4. **中文听写**：设语速/中文遍数/间隔/乱序 → 点「中文听写」→ 纸笔默写  
5. **英文跟读**：点「英文跟读」  

## 已知限制与坑

| 问题 | 说明 / 处理 |
|------|-------------|
| Windows 无中文语音 | 设置 → 时间和语言 → 语音 → 添加中文语音包 |
| 音质一般 | 系统 TTS 限制；后续可接 Edge TTS 等（非首版） |
| Chrome 需用户手势才播 | 首次点击「听写/跟读/按钮」后由用户触发 |
| React StrictMode 双挂载 | 开发模式可能重复创建 player；stop 在 cleanup 中处理 |
| Excel CSV 乱码 | 请另存为 **CSV UTF-8** |

## 实现中的注意点

1. `skip` 时必须 `cancelSpeak` + 清 gap 定时器，并用 `forceNext` 标记，由 loop 统一 `index++`，避免「跳过 + 循环自增」双跳。  
2. `rate` 限制在 0.5–1.5，避免极端值导致引擎异常。  
3. 切换教材时停止播放，避免队列指向旧词表。

## 后续可扩展（未做）

- 屏幕输入判分 / 错词本  
- 云 TTS 更高音质  
- 多教材打包下载  
- 桌面壳（Tauri）
